# Port for local development
PORT=3000

# The app now dynamically discovers the server URL via the browser host.
# You can override it here if needed:
# REACT_APP_SERVER_URL="http://192.168.29.179:3000"
